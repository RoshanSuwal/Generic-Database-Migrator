sparkConfiguration :
  configs :
    - key : spark.app.name
      value : cassandra migrator
    - key : spark.master
      value : local[*]
    - key : spark.submit.deployMode
      value : client

sourceProps:
  formats:
    - value : json

#  options :
#    - key : multiline
#      value : true

  load :  /home/roshan/workspace/dataset/yelp/yelp_academic_dataset_business.json

sinkProps:
  formats:
    - value: org.apache.spark.sql.cassandra
  modes:
    - value: append
  options:
    - key: spark.cassandra.connection.host
      value: 146.190.6.111
    - key: spark.cassandra.connection.port
      value: 32001
    - key: spark.cassandra.auth.username
      value: cassandra
    - key: spark.cassandra.auth.password
      value: cassandra